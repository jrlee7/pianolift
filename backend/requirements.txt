fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
# torchaudio >=2.9 drops built-in decoding (requires torchcodec+FFmpeg);
# pin 2.6 which still ships the soundfile backend piano_transcription_inference relies on.
#
# GPU machines: the pipeline auto-detects CUDA at runtime (pipeline.
# compute_device) — no code change needed, just install the CUDA builds
# instead of these CPU wheels:
#   pip install torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#   pip install "audio-separator[gpu]>=0.44.3"   (instead of [cpu])
# Everything (separation, both transcription engines) then runs on the
# GPU, and separation automatically uses a higher-quality overlap.
torch==2.6.0
torchaudio==2.6.0
# CPU-only piano source separation (BS-Roformer-SW, 6-stem). Piano SDR ~7.83
# vs ~2.23 for Demucs' htdemucs_6s, which bled badly with sustained/harmonic
# instruments (e.g. cello) into the piano stem.
audio-separator[cpu]>=0.44.3
piano_transcription_inference>=0.0.6
# Second transcription engine (semi-CRF, MIT; checkpoint ships in the
# wheel). Ensemble with the ByteDance model: agreement confirms notes,
# Transkun supplies key-release offsets + velocities (MAESTRO
# onset+offset+velocity F1 93.1 vs ~80.9) and an independent sustain-pedal
# detector used to verify pedal events.
transkun>=2.0.1
librosa>=0.10
mido>=1.3
# PDF page splitting for the per-page OMR fallback (omr.py): Audiveris
# refuses to export a book when any one page fails, so bad pages are split
# out and skipped individually.
pypdf>=4
soundfile>=0.12
# URL import (YouTube/Facebook/Instagram/...). Site extractors rot fast;
# keep this fresh when downloads start failing. [default] pulls yt-dlp-ejs,
# the JS challenge solver YouTube needs (runs via node or deno; one must be
# on PATH or downloads 403).
yt-dlp[default]>=2026.7.4
