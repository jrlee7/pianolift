fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
# torchaudio >=2.9 drops built-in decoding (requires torchcodec+FFmpeg);
# pin 2.6 which still ships the soundfile backend demucs relies on.
torch==2.6.0
torchaudio==2.6.0
# The demucs wheel can install incompletely (only __init__.py); force a
# source build. If demucs.separate is missing after install, re-run:
#   pip install --force-reinstall --no-deps --no-cache-dir --no-binary demucs demucs==4.0.1
--no-binary demucs
demucs==4.0.1
piano_transcription_inference>=0.0.6
librosa>=0.10
mido>=1.3
soundfile>=0.12
